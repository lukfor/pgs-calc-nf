// Global default params, used in configs
params {
    project = "test-gwas"
    input = "input"
    output = "output"
    pgs_scores = "PGS000013"
}

docker.enabled = true
process.container= 'lukfor/pgs-calc-nf:latest'

// default: resume
resume = true

// TODO: add profiles
